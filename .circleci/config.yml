version: 2
jobs:
  build: 
    docker:
      - image: circleci/node:4.8.2-jessie

    working_directory: ~/repo

    steps:
      - run: echo build step

  test:
    docker:
      - image: circleci/node:4.8.2-jessie

    working_directory: ~/repo

    steps:
      - run: echo test step

workflows:
  version: 2
  build-and-test: 
    jobs:
      - build
      - test:
          requires:
            - build

